{
  // ⚠️  SECURITY: Never commit real credentials here.
  // Provide values via environment variables or .NET User Secrets (dotnet user-secrets).
  //
  // Database:
  //   ConnectionStrings__DefaultConnection=Host=...;Database=...;Username=...;Password=...
  //
  // JWT:
  //   JwtSettings__Secret=$(openssl rand -base64 32)
  //
  // See docs/security_report.md — CRITICAL-001 for full remediation steps.
  "ConnectionStrings": {
    "DefaultConnection": ""
  },
  "JwtSettings": {
    "Secret": "",
    "Issuer": "IonCrm",
    "Audience": "IonCrmUsers",
    "AccessTokenExpiryMinutes": 15,
    "RefreshTokenExpiryDays": 7
  }
}
